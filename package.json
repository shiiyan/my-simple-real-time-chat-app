{
    "name": "my-simple-chat-app",
    "version": "1.0.0",
    "description": "",
    "engines": {
        "node": "16.17.0"
    },
    "scripts": {
        "heroku-postbuild": "cd backend && npm install --only=dev && npm run build"
    }
  }
  